go to backend run "npm i"
then start backend server from backend folder with "npm run dev"

go to analysis run "pip install -r requirements.txt"
then run analysis server with "uvicorn main:app --reload --port 8001"

go to front end run "npm i"
then run from front end "npm run dev"

enjoy 